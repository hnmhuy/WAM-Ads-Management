# Cleaning code requirements for officers

1. Đưa các file cần thiết về đúng folder của nó. Đối với các file css -> Mọi người tạo thêm folder cho mỗi phân hệ nha (trong folder css).
2. Đưa các file js về folder public/js
3. Sử đường dẫn nếu lỗi
4. Các file html tĩnh thì đưa về thư mục public/html
5. Data test cái handlebars template -> testing_view_data

    Nếu cái folder mà nó không có thì mn tạo giúp tui nhoa. Đặt tên file có ý nghĩa nha. Ko có định dạng tên file, mn chỉ cần để đúng folder cho gọn thoi là được. Code của mn thì gán clean nha, tui chịu! 🙁
